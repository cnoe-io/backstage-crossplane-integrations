apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: app-mod-backstage-templates
  description: CrossPlane Backstage Templates
spec:
  targets:
    - ./rds-cluster/template.yaml
    - ./app-with-bucket/template.yaml
